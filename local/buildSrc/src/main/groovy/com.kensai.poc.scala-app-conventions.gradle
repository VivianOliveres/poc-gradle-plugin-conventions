plugins {
    id 'com.kensai.poc.scala-common-conventions' // Apply com.kensai.poc.scala-common-conventions
}

ext {
    catsVersion = '2.3.0'
}

dependencies {
    // Use cats
    implementation "org.typelevel:cats-core_${scalaMajorVersion}:${catsVersion}"
}
